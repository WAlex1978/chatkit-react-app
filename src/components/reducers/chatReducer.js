const initState = {
    username: '',
}

const chatReducer = (state = initState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}

export default chatReducer;